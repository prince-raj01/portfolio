pages:
  stage: deploy
  image: alpine:latest
  script:
    - mkdir -p public
    - cp index.html resume.html public/
    - cp -r css js public/
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
