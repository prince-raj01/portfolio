pages:
  stage: deploy
  script:
    - mkdir public
    - cp -r index.html css js resume.html certificates README.md package.json public/ || true
    - rm -rf public/.git
  artifacts:
    paths:
      - public
  only:
    - main
